import React from 'react';
import { CreatePresentation } from '../../Projects/Create/CreatePresentation';

interface CreateDataProps {}

export const CreateData: React.FC<CreateDataProps> = ({}) => {
  return <CreatePresentation />;
};
